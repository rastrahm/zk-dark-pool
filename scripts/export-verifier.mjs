/**
 * Stub Fase 0 — exporta Groth16Verifier.sol (disponible desde Fase 4).
 * Uso: `npm run export:verifier`
 */
console.log("[export:verifier] stub Fase 0 — implementar en Fase 4 (export VK + pragma 0.8.24).");
process.exit(0);

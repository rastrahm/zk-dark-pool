/**
 * Stub Fase 0 — genera proof Groth16 + fixtures (disponible desde Fase 3).
 * Uso: `npm run generate:proof`
 */
console.log("[generate:proof] stub Fase 0 — implementar en Fase 3 (MatchOrders + ptau lab).");
process.exit(0);
